{
  "name": "video-processing-service",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/worker.ts",
    "start": "bun run src/worker.ts",
    "build": "bun build src/worker.ts --outdir dist --target bun --minify --sourcemap=linked",
    "test": "bun test"
  },
  "dependencies": {
    "@hls/shared": "workspace:*",
    "fluent-ffmpeg": "^2.1.3",
    "mongoose": "^9.3.0",
    "mongoose-paginate-v2": "^1.8.3",
    "pino": "^10.3.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/fluent-ffmpeg": "^2.1.28",
    "bun-types": "latest",
    "pino-pretty": "^13.0.0"
  }
}
